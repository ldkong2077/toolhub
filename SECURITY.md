# 安全策略

## 支持版本

最新发布的版本（带 `v*` 标签）会接收安全更新。

## 报告漏洞

如发现安全问题，**请勿**公开提交 Issue。请通过以下方式私密的反馈：

- GitHub 仓库的 **Security → Report a vulnerability**（漏洞上报）面板；
- 或通过仓库公布的联系方式联系维护者。

我们会在 **3 个工作日内**确认收到，并力争在 **7 个工作日内**给出修复时间表。

## 项目安全说明

- 本项目是**纯静态前端站点**：无后端、无 API Key、不收集任何用户数据。
- 所有工具链接均为 `src/lib/tools.ts` 中定义的外部公开资源。
- 构建过程可离线复现（使用系统字体栈，构建时不向外部网络发起请求）。
- 静态导出无法由应用本身输出安全响应头，请在托管层（Nginx / Cloudflare Pages / Netlify）补齐 CSP、X-Frame-Options、HSTS 等（见 `public/_headers` 与 `nginx.conf`）。
